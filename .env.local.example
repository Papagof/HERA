NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# service_role secret key (Project Settings > API) - server-only, never NEXT_PUBLIC_.
# Required for /users (invite/remove accounts). Bypasses RLS - keep out of the client bundle.
SUPABASE_SERVICE_ROLE_KEY=
